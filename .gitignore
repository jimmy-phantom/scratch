# Whitelist approach: ignore everything, then explicitly track persistent files

# Ignore everything by default
*

# Track persistent workspace structure
!.gitignore
!.cursorrules
!README.md

# Track persistent directories (and their contents)
!tools/
!tools/**
!templates/
!templates/**

# Track work/ directory and contents (managed via branches)
!work/
!work/**
